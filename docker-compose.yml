services:
  backend:
    image: insta-clip-backend
    build:
      context: ./backend
    ports:
      - '3001:3000'
    environment:
      NODE_ENV: production
      PORT: 3000
    restart: unless-stopped
