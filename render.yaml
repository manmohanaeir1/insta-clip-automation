services:
  - type: web
    name: insta-clip-backend
    runtime: docker
    dockerfile: backend/Dockerfile
    dockerContext: ./
    port: 3000
    envVars:
      - key: NODE_ENV
        value: production
